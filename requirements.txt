Flask==3.0.3
Flask-Cors==4.0.1
openpyxl==3.1.5
qrcode==7.4.2
Pillow==10.4.0
reportlab==4.2.5
Werkzeug==3.0.4
requests==2.32.3
cryptography==43.0.3
Flask-Limiter==3.8.0

# --- Adicionados para deploy em produção (Vercel + Neon PostgreSQL) ---
# psycopg2-binary: driver PostgreSQL. Usa a build binária (não exige
# compilador C nem libpq instalados no ambiente de build da Vercel).
psycopg2-binary==2.9.9
# gunicorn: não é usado pela Vercel (que tem seu próprio runtime
# serverless), mas mantém a app rodável como um servidor WSGI tradicional
# em qualquer outro host (Railway, Render, VPS, Docker) sem mudanças.
gunicorn==23.0.0
